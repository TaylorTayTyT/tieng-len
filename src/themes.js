import { createTheme} from '@material-ui/core/styles';

const topBar = createTheme({
    palette: {
        primary: {
            main: "#000000"
        },
        watermelonpink: {
            main: "#f2bdcb"
        },
        secondary: {
            main: "#daf3d4"
        }
    }
})

export default topBar