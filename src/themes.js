import { createTheme} from '@material-ui/core/styles';

const topBar = createTheme({
    palette: {
        primary: {
            main: "#000000"
        },
        watermelonpink: {
            main: "#f2bdcb"
        },
        secondary: {
            main: "#3e3830"
        }
    }
})

export default topBar