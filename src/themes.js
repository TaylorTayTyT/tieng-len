import { createTheme} from '@material-ui/core/styles';

const topBar = createTheme({
    palette: {
        primary: {
            main: "#FFFFFF"
        },
        watermelonpink: {
            main: "#f2bdcb"
        },
        secondary: {
            main: "#000000"
        }
    }
})

export default topBar